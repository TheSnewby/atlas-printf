#include "main.h"

/**
 * backslash - handles all backslashes and prints it properly
 * @c: character passed directly after backslash
 *
 * Return: 1 if printed, 0 if not
 */

int backslash(char c)
{
	char p; /* character to be printed if needed */
	if (c == 92)
		p = 92;
	else if (c == '')
		p = 1; /* might need to remove this function */
}
